/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["klbtheme.com", "www.sencor.com", "kontakt.az", "istorepreowned.co.za", "cellularpoint.ca"],
    },
};

export default nextConfig;
